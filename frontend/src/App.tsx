/*
 * App.tsx is the root component of the React application.
 * It renders the Offers component which displays available offers and payouts.
 * This is the main entry point for the frontend UI.
 */

import React from "react";
import Offers from "./Offers";

function App() {
  return <Offers />;
}

export default App;
